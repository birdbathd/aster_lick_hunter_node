# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# database
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm

# user configuration
config.user*
config.user.backup*.json
config.json
data/optimizer-jobs.json

# claude code local settings and agents
.claude/settings.local.json
.claude/agents/

# local development files (not for commit)
[WEB]
ecosystem.config.js
scripts/aster-notifier.cjs
*.swp
.*.swp
core

# Chat exports
ChatExport*/

# ═══════════════════════════════════════════════════════════════════
# Private / internal files — NOT for public repo
# ═══════════════════════════════════════════════════════════════════

# Personal analysis & conversation history (contains account balances, trade data)
CONVO
LIQUIDATION_ANALYSIS.md
OPUS_ANALYSIS_PROMPT.md
PR_DESCRIPTION.md

# Claude Code instructions (contains personal trade history, account details, strategy specifics)
CLAUDE.md

# Internal wiki (deployment-specific)
WIKI.md

# Aster exchange API docs (may be proprietary / not ours to redistribute)
docs/aster-finance-futures-api.md

# Per-deployment infrastructure
ecosystem.config.js
scripts/aster-notifier.cjs

# Symbols list (exchange-specific, changes frequently)
symbols.json

# Optimizer config (local tuning data)
optimize-config.js
KIMI_PROMPT.md
